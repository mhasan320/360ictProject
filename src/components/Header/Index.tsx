import React from 'react'
import { Typography } from 'antd';

const { Title } = Typography;

export default function Index() {
  return (
    <div className='header'>
        <Title level={2}>Rocket Launcher</Title>
    </div>
  )
}
